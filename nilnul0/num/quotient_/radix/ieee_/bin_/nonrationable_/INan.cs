namespace nilnul.num.quotient_.radix.ieee_.bin_.nonrationable_
{
	/// <summary>
	/// beyond infinite
	/// </summary>
	/// The highest-order bit in the significand specifies whether the NaN is a signaling or quiet one. The remaining bits of the significand form what is referred to as the payload of the NaN.
	/// So we have 2^53 -2 NaNs, among which as have 2^52-1 qNaN;
	public interface INan
		:biasE_.extrem_.fuly_.INan
	{ }
}
